{
    "signal": "SIGHUP",
    "env": {
        "NODE_ENV": "development"
    },
    "watch": ["favicon.ico", "src/"],
    "ext": "ts, js, txt, json, ico",
    "delay": 0,
    "exec": "vite build && SET PORT=3000 && node --inspect ./dist/app.js || exit 1"
}
